<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        table {
            border-collapse: collapse;
            width: 50%;
        }

        .show {
            border: 2px solid black;
            text-align: left;
            padding: 8px;
            font-family: Helvetica, Arial, sans-serif;
            width: 200;
        }

        a:link {
            color: rgb(54, 124, 43);

        }

        h1,
        h2,
        h3,
        h4,
        h5,
        h6,
        p,
        span,
        li {
            font-family: Helvetica, Arial, sans-serif;
            display: block;
            margin: 0;
            padding: 15px;
        }

        .even {
            background-color: #d8d8e7;
        }
    </style>
</head>

<body>
    <br />
    <h4>This is an autogenerated email please do not reply to EDL Core Team</h4>
    <br />
    <h4>Following {{catalogType}} {{#ifEqual status 'available'}}is{{else}}has been{{/ifEqual}} {{status}}{{#if
        rejected}} by <a href="mailto:{{rejectingApproverEmail}}" target="_top">{{rejectingApproverName}}</a>{{/if}}.
    </h4>
    <a href="{{dataUrl}}" target="_blank">
        <h3>{{name}}</h3>
    </a>
    <h4>This request was submitted by <a href="mailto:{{userEmail}}" target="_top">{{userName}}</a> on
        {{submissionDate}}.</h4>
    <br />
    <br />
    <br />
    <h3>Approval Status</h3>
    {{#if communityApproval}}
    <br />
    <table>
        <tr>
            <th class="show"><i>Community</i></th>
            <th class="show"><i>Status</i></th>
            {{#if rejected}}<th class="show"><i>Reason</i></th>{{/if}}
        </tr>
        {{#each communityApproval}}
        <tr {{#ifEven @index}} class="even" {{else}} class="odd" {{/ifEven}}>
            <td class="show">{{community}}</td>
            <td class="show">{{status}}</td>
            {{#if ../rejected}}<td class="show">{{reason}}</td>{{/if}}
        </tr>
        {{/each}}
    </table>
    {{/if}}
    {{#if subCommunityApproval}}
    <br />
    <table>
        <tr>
            <th class="show"><i>Sub Community</i></th>
            <th class="show"><i>Status</i></th>
            {{#if rejected}}<th class="show"><i>Reason</i></th>{{/if}}
        </tr>
        {{#each subCommunityApproval}}
        <tr {{#ifEven @index}} class="even" {{else}} class="odd" {{/ifEven}}>
            <td class="show">{{subCommunity}}</td>
            <td class="show">{{status}}</td>
            {{#if ../rejected}}<td class="show">{{reason}}</td>{{/if}}
        </tr>
        {{/each}}
    </table>
    {{/if}}
    <br />
    <br />
    <h4>To view all of your pending approvals, visit the <a href="{{approvalUrl}}" target="_blank">Approvals</a> page.
    </h4>
</body>

</html>